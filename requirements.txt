# MultimodalIntentFusion - Dependencies

# Core Speech-to-Text (using faster-whisper instead of whispercpp for better Windows support)
faster-whisper>=1.0.0

# Audio Processing & Prosody Analysis
librosa>=0.10.0
soundfile>=0.12.0
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0

# Emotion Detection (Audio)
scikit-learn>=1.3.0

# Facial Expression & Vision
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
deepface>=0.0.79
mediapipe==0.10.14

# Deep Learning (required by DeepFace)
tensorflow>=2.13.0
keras>=3.0.0

# Dependency conflict resolution
# MediaPipe 0.10.14 requires protobuf<5, TensorFlow requires protobuf>=5.28
# We pin to 4.25.3 which works with both
# protobuf==4.25.3

# LLM Integration (Local)
ollama>=0.1.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
click>=8.1.0

